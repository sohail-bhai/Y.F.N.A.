# Changes made:
# - Added VITE_API_BASE so frontend can construct backend URLs (uploads/Gmail evidence link).
# - Added VITE_COMPLAINT_RECIPIENT to prefill Gmail compose recipient.
VITE_API_BASE=http://localhost:8000
VITE_COMPLAINT_RECIPIENT=civic@municipality.gov.in
